document.querySelectorAll('.intro-social a').forEach(link => {
    link.setAttribute('target', '_blank');
});
document.querySelectorAll('.desc a').forEach(link => {
    link.setAttribute('target', '_blank');
});
document.querySelectorAll('.footer-social a').forEach(link => {
    link.setAttribute('target', '_blank');
});
